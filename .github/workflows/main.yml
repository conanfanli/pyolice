
- name: Publish package to TestPyPI
  on:
    push:
      branches:
        - master
  uses: pypa/gh-action-pypi-publish@master
  with:
    user: __token__
    password: ${{ secrets.pypi_token }}
    verbose: true
